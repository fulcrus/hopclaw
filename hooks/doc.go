// Package hooks manages lifecycle hooks and their persisted registrations.
//
// It provides catalog, filtering, storage, and plugin-sync support for hook
// assets that extend HopClaw runtime behavior around key events.
package hooks

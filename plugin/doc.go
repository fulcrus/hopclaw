// Package plugin discovers, loads, and normalizes plugin manifests.
//
// It turns plugin directories into inspectable runtime components such as
// providers, channels, tools, MCP servers, commands, and agent presets.
package plugin
